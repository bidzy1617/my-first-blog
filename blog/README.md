python 

```
#installing django through a proxy server
when installing through a server use the command proxy followed by the proxy address to install
pip install --proxy *your proxy here* *package to be installed*
>do not use the command line in Visual Studio



```